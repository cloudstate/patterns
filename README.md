# Patterns
A demonstrator of different styles of coding patterns


### Clause
The clause pattern demonstrates one way how to code complex boolean expressions in a readable fashion.

```
gradle dependencyUpdates -Drevision=release --no-parallel
```